# frozen_string_literal: true

RSpec.describe Gemika::Json::Ld::Images do
  it "has a version number" do
    expect(Gemika::Json::Ld::Images::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
